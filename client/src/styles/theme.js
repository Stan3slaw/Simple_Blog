import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#4683d9',
    },
    secondary: {
      main: '#fff',
      contrastText: '#4683d9',
    },
  },
});

export default theme;
